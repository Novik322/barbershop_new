<Page x:Class="BarberShop_Samarin.ClinentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BarberShop_Samarin"
      mc:Ignorable="d" 
      d:DesignHeight="665" d:DesignWidth="1000"
      Title="ClinentsPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="10*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="5">
            <Button Margin="0 0 10 0" Content="Добавить клиента" Style="{StaticResource AcceptButton}"/>
            <Button Margin="0 0 10 0" Content="Удалить клиента" Style="{StaticResource AcceptButton}"/>
            <Button Margin="0 0 10 0" Content="Изменить клиента" Style="{StaticResource AcceptButton}"/>
        </StackPanel>
        <Menu Grid.Row="1" Grid.Column="1" Opacity="2">
            <MenuItem>
            </MenuItem>
        </Menu>
    </Grid>
</Page>
